// unit.id → ability id (или нормализованное англ. имя wiki-способности: lowercase, только [a-z0-9]),
// которые не должны показываться юниту.
// Аудит British: все 35 game-ability ids (14 юнитов) проверены по RGD —
// каждый присутствует в squad_ability_ext своего отряда (полный парс таблиц,
// включая дублирующиеся ключи, которые теряет базовый парсер). Ложных способностей
// нет: способности «Не стрелять»/маскировки/ремонта/гранат реально лежат в
// squad_ability_ext, а не в апгрейдах. Wiki-способностей у British в wiki.ts нет
// (unitIndex 36–62, 159–167 отсутствуют). Способности, выдаваемые доктриной, не удаляются.
export const abilityExclusionsBritish: Record<string, string[]> = {};
